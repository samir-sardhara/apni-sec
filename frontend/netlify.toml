[build]
  command = "npm install && npm run build"
  publish = "dist"

# Redirect API calls to backend (update with your backend URL)
[[redirects]]
  from = "/api/*"
  to = "https://your-backend-url.railway.app/api/:splat"
  status = 200
  force = true

[build.environment]
  NODE_VERSION = "18"

